bin/
obj/
out/
.vs/
*.user
local.props
.env
.env.*
node_modules/
.vercel/
*.pfx
*.pem
dist/

# Рабочие файлы автора в корне проекта (скриншоты, архивы) — в репозиторий не нужны
*.zip
Screen Shot*.png
strassio-stone-256.png

# Скриншоты Windows (Win+Shift+S → вставка в папку) — имя вида {GUID}.png
{*}.png

# Личные настройки Claude Code на этом компьютере
.claude/settings.local.json

# Interop CorelDRAW — файл Corel, публиковать нельзя. Лежит только локально (см. README).
src/Strassio.Corel/interop/*.dll
