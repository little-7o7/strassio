# История изменений Strassio

Здесь простыми словами записывается, что изменилось в каждой версии.

## [не выпущено]
- Создано ТЗ (docs/SPEC.md), правила проекта (CLAUDE.md) и иконка кнопки-камня.
- Этап 0 (черновик, ждёт проверки автором в CorelDRAW):
  - Починены ошибки в заготовке проекта (не совпадало имя файла интеропа; тестовый и
    Preview-проекты были нацелены на несуществующий .NET 10 — переключены на .NET 8).
  - Проверено: все 5 проектов (`Strassio.Core`, `Strassio.Licensing`, `Strassio.Corel`,
    тесты, Preview) собираются одной командой `dotnet build` без ошибок.
  - Добавлен пустой докер Strassio (WPF) с одной тестовой кнопкой «Создать тестовую стразу (ss6)» —
    создаёt один круг диаметром 2,4 мм на активном документе, одной группой отмены (Ctrl+Z).
  - Добавлены `AppUI.xslt` / `UserUI.xslt` — кнопка-камень, своя панель инструментов «Strassio»,
    пункт в меню «Окно → Докеры → Strassio». Формат сверен с открытыми рабочими аддонами bonus630
    для CorelDRAW X7+, подробности и на что смотреть при проверке — в docs/SPEC.md, раздел 18.
  - Значок камня на кнопке пока не подключён (нужна отдельная небольшая задача — компиляция
    ресурса CorelDRAW под иконку). Кнопка пока с подписью «Strassio» без картинки.
  - Проверено автором в реальном CorelDRAW: панель «Strassio», докер открывается вкладкой справа,
    кнопка «Создать тестовую стразу (ss6)» создаёт круг 2,4 мм, отмена одним Ctrl+Z — всё работает.
  - **Этап 0 закрыт.** Дальше — Этап 1 (ядро): модели, таблица камней, стразы по линии L1–L3
    с углами, заливка F1–F5, исправление пересечений, Preview-инструмент, тесты.
- Этап 1 (в работе):
  - Геометрия (`Strassio.Core.Geometry`): точки, отрезки и кривые Безье, разбивка кривой в
    полилинию с адаптивной точностью, поиск острых углов с настраиваемым порогом — работает и
    на замкнутых кривых (стык не даёт лишнего угла).
  - Метод **L1 «по линии»** (`Strassio.Core.Placement.LineScatterer`): зазор, отступ от начала
    и конца, направление, три режима шага (подгонка / точный шаг / точное количество). В каждом
    остром угле — страза точно в вершине, соседние отрезки заполняются независимо, без нахлёста
    и без дырок в углах. 16 xUnit-тестов.
  - Preview-инструмент (`dotnet run --project tools/Strassio.Preview -- <line|zigzag|square|star>`)
    рисует результат в SVG (`out/preview/`) — проверено визуально на всех четырёх сценариях,
    острые углы (красным) стоят точно в вершинах. Сценарии «сердце», «тугая спираль», «S-кривая»,
    «буквы» из обязательного списка (SPEC 6.5) пока не добавлены — сделаем перед началом работы
    над исправлением пересечений (раздел 6 ТЗ), где они нужнее всего.
  - **Смещение контура** (`Strassio.Core.Geometry.CurveOffsetter`) — параллельная кривая на
    расстоянии, нужна методам L2 и L3. Снаружи угла — дуга, изнутри — обрезка (петля вырезается
    ДО расстановки камней, раздел 6.1 ТЗ). На очень острых углах (тонкий шип звезды, ~35°)
    добавлено ограничение митра (аналог stroke-miterlimit) — без него обрезка улетала на 20 мм
    вместо разумных 6, ломая контур. Проверено на Preview (`offset-square`, `offset-star`).
  - **Микро-сдвиг у очень острых углов (вместо дыры или растяжения ряда).** Замечено на
    Preview-звезде: в острых углах (~35°) два соседних камня с разных сторон угла физически
    перекрывались (1,6 мм между центрами при диаметре 2,4 мм). Перепробованы два варианта, оба
    отклонены автором по фото: (1) локальный отступ у вершины — оставлял заметную дыру в ряду;
    (2) равномерное уменьшение шага по всему отрезку — ряд становился заметно реже, автор сравнил
    со скриншотом реального инструмента (rStones) и своей ручной правкой — там ряд остаётся частым.
    Первая версия точечного сдвига (одна страза с каждой стороны, до полного обычного зазора ряда)
    автор тоже отклонил по второму фото (сравнение со своей ручной правкой и rStones) — сдвиг был
    виден, а зазор у самой вершины должен быть заметно меньше обычного. Доработано:
    1) у острого угла целимся не в обычный зазор ряда, а в маленький `CornerMinGapMm` (по умолчанию
       0,1 мм — у самой вершины это не бросается в глаза, а лишний сдвиг наоборот заметнее);
    2) необходимый сдвиг распределяется не на одну стразу, а плавно (по косинусу, вес от 1 до 0)
       на несколько страз подряд с каждой стороны угла — `CornerTaperCount` (по умолчанию 4), —
       получается пологий незаметный изгиб ряда, а не один резкий скачок.
    Сдвиг любой отдельной стразы по-прежнему не больше `MaxCornerNudgeMm` (1 мм). Сама угловая
    страза остаётся точно в вершине (раздел 4 ТЗ) — количество страз не меняется, ряд остаётся
    полностью частым. Добавлен тест на саму плавность (сдвиг соседних страз должен монотонно
    убывать по мере удаления от угла).
  - **Опорные горизонтальные/вертикальные линии не трогаем совсем.** Замечено на «плечах» звезды
    (почти горизонтальный отрезок между острым углом и соседней вершиной): 50/50 сдвиг между двумя
    отрезками угла делал заметно кривой и строго горизонтальную линию — глаз сразу цепляется за
    отклонение от горизонтали/вертикали, даже маленькое, в отличие от того же отклонения на уже
    наклонном отрезке. Теперь доля сдвига между двумя отрезками угла не всегда 50/50: чем ближе
    отрезок к строго горизонтальному/вертикальному, тем меньше его доля; если он строго горизонтален
    или вертикален — доля ровно 0, весь сдвиг плавно забирает на себя соседний (уже наклонный)
    отрезок. Проверено на Preview: «плечи» звезды остались идеально прямыми. 25 тестов проходят.
  - **Метод L1 подключён к CorelDRAW (тестовая кнопка).** Раз алгоритм L1 уже проверен тестами
    и на глаз через Preview (правило CLAUDE.md — сначала SVG, потом CorelDRAW), в докере появилась
    кнопка «Обвести выделенную кривую (L1, тест)»: читает узлы и контрольные точки Безье выделенной
    линии (один раз, как требует CLAUDE.md), строит из них кривую Strassio.Core, считает стразы
    методом L1 (ss6, подгонка) и создаёт круги одной группой отмены. Выбор размера/цвета/метода
    в интерфейсе появится на Этапе 2 — сейчас параметры зашиты в коде для проверки.
  - **Методы L2 «вокруг линии» и L3 «по смещённой кривой»** (`Strassio.Core.Placement.RingScatterer`):
    один или несколько рядов, каждый — своя смещённая копия исходной кривой (через уже готовый
    `CurveOffsetter`) со своими параметрами расстановки — свой размер камня, свой зазор, свой
    шахматный сдвиг (`RowSpec.ScatterOptions.StartOffsetMm`) на ряд, как требует раздел 4 ТЗ. L3 —
    это вызов с одним рядом; L2 — с несколькими, в одну или обе стороны. У `CurveOffsetter` появился
    выбор стиля угла — дуга (по умолчанию) или срез (митр), тоже раздел 4 ТЗ. Вся защита от острых
    углов (плавный сдвиг, опорные линии) работает в каждом ряду автоматически — она уже часть L1,
    на который опирается каждый ряд. Проверено на Preview (`ring-square`, `ring-star`, 3 ряда,
    средний крупнее): ни одного наложения даже на самых острых концах звезды. 5 новых тестов
    (29 всего).
  - **Исправление пересечений (базовая версия)** (`Strassio.Core.Placement.IntersectionFixer`,
    раздел 6 ТЗ). Автор заметил наложение на Preview `ring-star`: три ряда L2 у острых концов
    звезды физически перекрывались — плавный сдвиг в LineScatterer чинит только два отрезка ОДНОГО
    угла в ОДНОМ ряду, а разные ряды друг о друге ничего не знают. Новый общий алгоритм проверяет
    ЛЮБЫЕ две стразы (из разных рядов, разных методов) через пространственную сетку (раздел 6.2,
    не перебором всех пар — 5000 страз с гарантированными пересечениями решаются меньше чем
    за секунду) и убирает те, что ниже приоритетом: угловые → крупнее → раньше в списке (раздел 6.3).
    На `ring-star` (3 ряда, шаг 3,4 мм, угол в острие ~37°) убрано 105 из 340 страз — там, где три
    ряда физически не помещаются различимо у самого острия; проверено визуально — выглядит чисто,
    без дыр и наложений. Действия «сдвинуть» и «только показать» (раздел 6.4) — пока не реализованы,
    только «удалить».
  - **Поправка: убраны «одиночки», рвавшие край ряда.** Первая версия исправления пересечений
    (без наложений) на деле выглядела плохо — по замечанию автора, проверил вблизи и согласился:
    отдельные стразы оставались висеть по одной между двумя дырками, край ряда получался рваным,
    а не ровно суженным. У каждой стразы появился номер ряда (`PlacedStone.RowId`, проставляет
    `RingScatterer`), и добавлен второй проход: страза, у которой сосед до и после в ЕЁ ЖЕ ряду уже
    убраны, тоже убирается — так дырки сливаются в один аккуратный суженный участок вместо рваного
    края. На `ring-star` теперь убирается 150 из 340 (было 105) — ряды по краям чисто сужаются
    к самым острым концам, у вершины остаётся только приоритетный центральный ряд; проверено
    визуально — выглядит как задуманное сужение, а не поломка. 8 новых тестов (37 всего).
  - **Заливка формы: F1 «сетка» и F2 «соты»** (`Strassio.Core.Placement.GridFiller`, раздел 5 ТЗ).
    Сначала — проверка «точка в контуре» (`PointInPolygon`, луч вправо + чётность пересечений,
    работает и с вогнутыми формами вроде звезды) и «расстояние до края» (для отступа). Форма может
    состоять из нескольких контуров — внешняя граница и отверстия (буквы «О», «А», кольца) —
    посчитано по чётно-нечётному правилу: точка внутри формы, если внутри нечётного числа контуров.
    Заливка: сетка с углом поворота, обычная (квадратная) или плотная (соты, гексагональная
    упаковка), с отступом от края. Проверено на Preview (`fill-square`, `fill-honeycomb`,
    `fill-star`, `fill-letter-o`) — дырка в «О» остаётся пустой, звезда заливается без наложений
    и без вылезания за край. 10 новых тестов (47 всего).
  - **Заливка формы: F3 «контурная», F4 «комбинированная», F5 «кант»** (`ContourFiller`, раздел 5
    ТЗ). Концентрические ряды от края внутрь (каждый через уже готовые CurveOffsetter+LineScatterer),
    пока помещаются; F3 — рядов сколько влезет плюс сеткой добивается центр, F5 «кант» — только
    N рядов, внутри пусто, F4 — N рядов по краю плюс сетка внутри. Ряды могут перекрываться друг
    с другом на сложных фигурах — в конце всегда проходит `IntersectionFixer`.
    При тестировании поймал зависание (тесты не завершались за 120 c): при сильном сжатии внутрь
    фигура самопересекалась, а проверка «кольцо ещё имеет смысл» по одной только площади это не
    ловила — добавлена вторая проверка по ограничивающему прямоугольнику и разумный потолок числа
    колец вместо фиксированных 2000. Также нашёл настоящую дыру ровно в центре квадрата — оказалось,
    я по ошибке пометил плоскую 2D-сетку тем же «рядом» (`RowId`), что и кольца, и проход «убрать
    одиночку» (которому нужна последовательность вдоль линии) принял точки сетки за такую
    последовательность и снёс стразу в центре; у сетки теперь особый `RowId = -1`, который этот
    проход не трогает. Проверено на Preview (`contour-square`, `contour-star`, `contour-edge-only`,
    `contour-combined`) — без дыр и наложений. 5 новых тестов (52 всего).
  - **L2/L3 и заливка подключены к CorelDRAW (тестовые кнопки).** Три новые кнопки в докере (нужна
    выделенная ЗАМКНУТАЯ фигура): «Кольца вокруг фигуры» (L2 — 3 ряда, центр ss8 крупнее краёв ss6),
    «Залить сотами» (F2), «Залить контурно» (F3). Общий код чтения кривой и создания кругов вынесен
    в один метод (раньше дублировался для каждой кнопки).
  - **Исправлен баг метода L2 на маленьком лепестке (по замечанию автора из реального CorelDRAW).**
    Когда смещение внутреннего ряда больше половины ширины самой фигуры, контур смещения
    «выворачивался наизнанку» — по площади и ограничивающему прямоугольнику это выглядело как
    обычная фигура, поэтому старая проверка (раздел 6 ТЗ) его пропускала, и стразы всех рядов
    сваливались в кучу друг на друга. Добавлена проверка расстояния смещения напрямую относительно
    размера ИСХОДНОЙ фигуры — такой ряд теперь просто тихо пропускается, а не превращается в кашу
    из наложенных кругов. Общие геометрические функции (площадь, ограничивающий прямоугольник)
    вынесены в `Strassio.Core.Geometry.CurveMetrics`, чтобы использовать одну и ту же проверку
    в `RingScatterer` и `ContourFiller`. Добавлены недостающие обязательные сценарии Preview
    (раздел 6.5 ТЗ): «сердце», «тугая спираль», «S-кривая», «буквы» (гантель), плюс «лепесток»
    (обычный, острый, маленький — тот самый баг). 1 новый тест (53 всего).
  - **Проверка Этапа 1 целиком.** Все 25 сценариев Preview прогнаны заново, и каждая картинка
    проверена не на глаз, а числами: для всех пар страз посчитано расстояние между центрами.
    Наложений нет нигде. Так нашёлся настоящий баг (см. ниже).
  - **Исправлено: в вершине стыка замкнутой кривой оказывались две стразы друг на друге.**
    Если у замкнутой кривой последний узел совпадает с первым — а CorelDRAW почти всегда отдаёт
    их именно так — этот стык считался острым углом дважды: и в начале кривой, и в конце.
    Заметно было бы прямо в работе: в одной точке два круга вместо одного. Теперь при разбивке
    кривой вырожденные (нулевой длины) участки убираются, а стык замкнутой кривой сводится
    ровно в одну точку. 2 новых теста (55 всего).
  - **Аддон читает все контуры выделенной фигуры.** Раньше брался только первый — у буквы «О»
    или кольца дырка заливалась стразами. Теперь заливка сеткой и сотами получает все контуры
    сразу и оставляет отверстия пустыми, а методы по линии берут внешний контур.
  - **Установщик `Strassio-Setup.exe`** (раздел 14.2 ТЗ, сделан раньше плана — чтобы было
    чем пробовать). Сам находит все установленные CorelDRAW и показывает списком с галочками,
    ставит плагин в отмеченные, не даёт ставить поверх запущенного CorelDRAW, удаляется через
    «Программы и компоненты» и при удалении не трогает настройки в `%APPDATA%\Strassio`.
    Русский и английский. Собирается командой `installer\build-installer.ps1`.
    Код поиска версий проверен на этом компьютере — находит CorelDRAW 2018, 25 и 26.
  - **Контурная заливка (F3/F4/F5) переделана — ряды больше не путаются на звезде.** Раньше каждый
    ряд строился «сжатием» контура, и на фигурах с тонкими местами (лучи звезды) контур
    выворачивался наизнанку: ряды налезали друг на друга и оставляли дыры. Теперь сначала
    строится карта «сколько отсюда до края», и ряд — это линия, где до края везде одинаково.
    Такая линия не может запутаться и сама делится на несколько колец там, где делится форма.
    Что ещё сделано:
    - Ряды идут и вокруг отверстий (буква «О», кольцо). Кнопка «Залить контурно» в CorelDRAW
      теперь передаёт все контуры фигуры, а не только внешний.
    - Последний проход «добить пустые места»: если в середине, где сходятся ряды, осталась
      дыра, в которую влезает страза, она туда ставится. Так нашлась и закрылась дыра в центре звезды.
    - Исправлено: маленькая дырка буквы «О» могла ограничить число рядов всей фигуры.
    - Скорость: 10 000 страз считаются примерно за 0,35 секунды.
    - Превью контурной заливки теперь само считает наложения и пустые места. Новые сценарии:
      `contour-letter-o`, `contour-heart`, `contour-letters`. На всех 7 сценариях наложений 0,
      пустых мест 0 (кроме «канта», где середина пустая по замыслу).
    - 10 новых тестов (65 всего).
  - **При пересечении теперь три действия на выбор (раздел 6.4 ТЗ), а не одно «удалить».**
    - «Удалить» — как раньше: менее важная страза убирается.
    - «Сдвинуть» — мешающая страза сначала пробует отодвинуться вдоль своей же линии (по умолчанию
      не дальше 1 мм, шаг поиска 0,05 мм) и не налезая на соседей по ряду; не получилось —
      тогда убирается. Угловые стразы не двигаются никогда: угол должен остаться углом.
      Стразы плоской сетки тоже не двигаются — у них нет «своей линии».
      На звезде (три ряда, `ring-star`) сдвиг сохраняет 197 страз вместо 190 при удалении.
    - «Только показать» — ничего не меняет, а возвращает список всех страз, которые с кем-то
      накладываются (обе стразы каждой пары), чтобы их можно было подсветить в CorelDRAW.
    - Проверено на всех 5 кольцевых сценариях Preview: наложений 0 во всех режимах. Рядом с
      `<сценарий>-fixed.svg` теперь сохраняется и `<сценарий>-shift.svg` для сравнения на глаз.
    - 8 новых тестов (73 всего).
  - **После удаления соседи по ряду раздвигаются — дырки не остаётся (раздел 6.4 ТЗ).**
    - Работает в «Удалить» и «Сдвинуть». На месте убранной стразы берётся от 1 до 6 соседей с
      каждой стороны, и они расставляются равномерно вдоль линии ряда; крайние стоят на месте.
    - Шаг ряда при этом может вырасти не больше чем на четверть диаметра стразы. Если больше —
      это не дырка, а место, где ряды сходятся (там стоит чужая страза), и его не трогаем.
    - Угловые стразы не двигаются. Если раздвинутые соседи налезли бы на другой ряд — раздвигание
      отменяется. Работает и на замкнутых контурах, в том числе на «шве» (между последней и первой).
    - На звезде (`ring-star`) раздвинуто 28 соседей, на лепестке (`ring-petal`) — 4; наложений 0
      во всех кольцевых сценариях. `<сценарий>-fixed.svg` в Preview теперь показывает именно это.
    - 6 новых тестов (79 всего).
  - Ещё не сделано в разделе 6.4 ТЗ: выбор действия («удалить / сдвинуть / только показать») ещё
    не выведен в докер — в CorelDRAW пока всегда работает «удалить» (уже с раздвиганием соседей).
  - **Этап 1 закрыт.**

- Этап 2 (в работе, ждёт проверки автором в CorelDRAW):
  - **Новый докер вместо тестовых кнопок.** Сверху вниз: название и шестерёнка настроек; выбор камня —
    размер списком («ss6 — 2,4 мм») и цвет кружками-образцами; вкладки «Линия» и «Заливка» со списком
    методов и короткой подсказкой под ним; большая кнопка «Создать»; строка состояния.
    - Методы: «По линии», «Вокруг линии (3 ряда)», «Сетка», «Соты», «Контурная». Все работают выбранным
      камнем: круги нужного диаметра, залиты цветом камня, у каждого круга имя вида `ss6 Красный`, вся
      операция — одна группа с именем `Strassio: линия ss6 Красный` и одна отмена Ctrl+Z.
    - Последний выбранный камень запоминается и выбирается сам при следующем запуске.
    - Параметры методов (зазор, число рядов и т.д.) пока зашиты: зазор 0,2 мм. Поля для них — следующий шаг.
  - **Таблица камней в файле** `%APPDATA%\Strassio\stones.json` (раздел 3.2 ТЗ). При первом запуске
    создаётся стартовая: ss5 — 2,1 мм, ss6 — 2,4 мм, ss8 — 2,5 мм, у каждого — кристалл, красный,
    зелёный, золото. Пока её можно править руками в файле; редактор таблицы в плагине — следующие шаги.
    Если файл испорчен, он не теряется: переименовывается в `stones.json.broken-<дата>`.
  - **Два языка — русский и английский** (`lang/ru.json`, `lang/en.json`). Все тексты докера и окна
    настроек — только из этих файлов. Язык меняется сразу, без перезапуска CorelDRAW. Новый язык
    добавляется одним файлом — он сам появится в списке.
  - **Темы как у CorelDRAW.** Докер и окно настроек берут цвета текущей цветовой схемы CorelDRAW
    (4 схемы: самая светлая, средне-светлая, тёмная, чёрная) и сами перекрашиваются, как только вы
    меняете схему в CorelDRAW. В настройках можно вместо этого выбрать «Светлая» или «Тёмная». Если
    CorelDRAW не сообщает схему (X7), берётся тема Windows.
  - **Окно настроек** (шестерёнка): язык, тема, единицы (мм/дюймы), знаков после запятой, обводка
    страз (нет / волосяная / заданной толщины) и кнопка «Папка настроек». Сохраняется в
    `%APPDATA%\Strassio\settings.json`.
  - Установщик теперь кладёт и папку `lang`.
  - **Исправлено (по скриншоту автора): вместо текстов в докере были видны ключи вида `[docker.create]`.**
    Установщик раскладывает файлы по версиям CorelDRAW по своему списку, и папки `lang` в нём не было.
    Теперь копируются все файлы языков. Кроме того, оба языка встроены прямо в `Strassio.Corel.dll`
    как запас: если папки `lang` нет, тексты всё равно на месте. Файл в папке главнее — его можно
    поправить без пересборки.
  - **Исправлено: в таблице камней вместо названий цветов были ключи** (`[stones.default.red]`) — из-за
    той же ошибки. Плагин сам заменяет такие названия на нормальные («Красный», «Золото»…) при
    запуске и сохраняет файл; названия, которые вы вводили сами, не трогаются.
  - Установщик — версия 0.1.3.
  - 34 новых теста (113 всего): языки, настройки, мм/дюймы, цвета, выбор темы.
  - **Исправлено (по скриншоту автора): в CorelDRAW 2026 докер был тёмным, хотя CorelDRAW светлый.**
    В CorelDRAW 2026 новый интерфейс, и его цветовая схема называется по-новому (`Scheme_11_ModernUI`).
    Плагин этого названия не знал и брал тему Windows, а она у автора тёмная. Теперь у плагина
    есть палитра нового светлого интерфейса CorelDRAW 2026 (цвета сняты со скриншота). Если в будущих
    версиях появится схема, в названии которой есть «Dark» или «Black», докер тоже станет тёмным.
  - Установщик — версия 0.1.4.
  - **Параметры методов в докере.** Под подсказкой метода — блок «Параметры»: только поля выбранного
    метода, у каждого поля — подсказка при наведении. Все значения запоминаются между запусками, как и
    последний выбранный метод. Длины показываются и вводятся в мм или дюймах (как в настройках),
    можно писать и через запятую, и через точку. Неверное число — рамка поля краснеет, внизу написано,
    какие числа подходят, и «Создать» не сработает, пока не исправить.
    - «По линии»: зазор; шаг — подгонка / точный шаг / точное количество; отступ от начала; отступ от
      концов; в обратную сторону; порог угла.
    - «Вокруг линии» (было «3 ряда»): зазор; число рядов; зазор между рядами; ряды в обе стороны /
      наружу / внутрь; шахматный сдвиг; углы ряда круглые / острые; наложения — удалить лишние /
      сдвинуть, иначе удалить / только показать (налезающие стразы остаются с красной обводкой);
      порог угла. Теперь работает и на незамкнутой линии.
    - **Новый метод «По смещённой линии»**: ряд на заданном расстоянии наружу или внутрь, углы
      круглые или острые. Наложения у острых углов убираются.
    - «Сетка» и «Соты»: зазор, угол сетки, отступ от края.
    - «Контурная»: зазор, отступ от края, чем заполнить середину (соты / сетка).
    - **Новые методы «Комбинированная»** (N рядов по краю + соты или сетка внутри) **и «Кант»**
      (только N рядов по краю).
  - Preview: сценарий `methods` рисует все методы на звезде, сердце, квадрате, S-кривой и зигзаге
    (`out/preview/methods/`). Наложений 0 во всех картинках.
  - Установщик — версия 0.1.5.
  - 29 новых тестов (142 всего).
  - **Редактор таблицы камней прямо в плагине** (раздел 3.1 ТЗ). Кнопка-карандаш рядом со списком
    размеров открывает окно «Таблица камней»: размеры слева, цвета выбранного размера справа.
    - Размер: название и диаметр (в мм или дюймах, как в настройках). Добавить (новый размер сразу
      получает цвета выбранного), удалить, выше/ниже, упорядочить по диаметру.
    - Цвет: название и цвет — можно ввести `#E53935`, `E53935`, `#E53` или `229, 57, 53`, либо
      выбрать в стандартном окне выбора цвета Windows. Добавить, удалить, выше/ниже.
    - «Во все размеры» — скопировать цвета размера во все остальные (одноимённые обновятся,
      недостающие добавятся).
    - Всё правится в копии: «Отмена» ничего не меняет. По «ОК» таблица проверяется (пустые названия,
      повторы, диаметр от 0,5 до 30 мм, размер без цветов, непонятный цвет) — ошибки показываются
      внизу окна, и пока их не исправить, таблица не сохранится. После «ОК» докер сразу показывает
      новые размеры и цвета.
  - Новый тест: каждый текст, который упоминается в докере и окнах, есть в обоих языках —
    чтобы в CorelDRAW нигде не появилось `[ключ]` вместо текста.
  - 27 новых тестов (169 всего).
  - **Экспорт и импорт настроек** (раздел 12 ТЗ). В окне настроек — кнопки «Экспорт…» и «Импорт…».
    Экспорт сохраняет настройки (язык, тема, единицы, обводка, параметры методов, последний камень и
    метод) и таблицу камней в один файл `.strassio` — перенести на другой компьютер или раздать
    сотрудникам. Импорт сразу применяет файл: язык, тема, таблица камней и докер меняются без
    перезапуска. Чужой файл, файл от более новой версии плагина или файл с ошибками в таблице камней
    не загружается — рабочие настройки не портятся, внизу окна написано, что не так.
  - 6 новых тестов (175 всего).
  - **Предпросмотр** (раздел 2.2 ТЗ). Рядом с «Создать» — кнопка «Предпросмотр»: тот же расчёт, но
    результат рисуется картинкой прямо в докере (контур фигуры серым, стразы цветом камня), а документ
    не меняется. Можно поменять параметры и нажать ещё раз. При «Только показать» налезающие стразы
    на картинке с красной обводкой. Картинка убирается при смене метода и при «Создать».
    Живой предпросмотр (обновляется сам при каждом изменении) — Этап 4.
  - **Картинки-схемы у методов** (раздел 2.2 ТЗ). В списке «Метод» у каждого метода маленькая
    картинка: волна для «По линии», три ряда для «Вокруг линии», кольцо внутри круга для «По смещённой
    линии», сетка, соты, контурная, комбинированная и кант на круге. Картинки не нарисованы вручную —
    плагин считает их тем же методом на маленькой фигуре, поэтому они честно показывают, что метод
    делает. Посмотреть крупно: `dotnet run --project tools/Strassio.Preview -- icons` → `out/preview/icons/`.
  - 2 новых теста (177 всего).
  - **Горизонтальная раскладка докера** (разделы 2.2 и 12 ТЗ). В настройках — «Раскладка докера»:
    вертикальная (блоки друг под другом, как раньше) или горизонтальная (камень, метод с параметрами
    и кнопки — три блока рядом, для докера внизу или сверху окна). Меняется сразу, без перезапуска.
    Если докер уже примерно 660 точек, появляется прокрутка вбок, а поля не обрезаются.
  - Кнопка «Предпросмотр» больше не обрезает свою надпись в узком докере.
  - Для разработки: `tools/Strassio.UiShots` снимает докер и окна без CorelDRAW в `out/ui/`
    (светлая и тёмная тема, обе раскладки) — так Claude проверяет внешний вид, раз CorelDRAW ему не
    запустить. Настоящие настройки при этом не трогаются. В README — новый скриншот докера.
  - Установщик — версия 0.1.6.

- Этап 3 (готов, ждёт проверки автором в CorelDRAW):
  - **Вкладка «Правка»** (раздел 7 ТЗ). Работает с выделенными стразами, а если стразы не выделены —
    со всеми стразами на странице (внутри групп тоже). Всё — одной отменой Ctrl+Z.
    - **Пять режимов удаления** (7.1): «Оставить верхние, удалить нижние», «Удалить верхние, оставить
      нижние», «Удалить по линии» (выделите линию-«нож» — все стразы, которые она пересекает,
      удалятся), «Удалить внутри формы» / «Удалить снаружи формы» (выделите замкнутую форму; галочка
      «И задевающие край формы»), «Сдвинуть вместо удаления» (мешающая страза встаёт на свободное
      место рядом, не дальше половины диаметра; нет места — удаляется).
    - **Удалить дубли** (7.2) — стразы точно друг на друге после копирования; из каждой кучки остаётся
      одна (верхняя или закреплённая).
    - **Закрепить / Открепить** (7.2) — закреплённые стразы правка никогда не удаляет и не двигает.
      Объекты, заблокированные средствами самого CorelDRAW, тоже не трогаются.
  - **Вкладка «Цвет»** (раздел 3.4): «Перекрасить выделенные в „…“» (цвет — выбранный вверху; имя
    стразы становится «размер новый цвет») и «Выделить на странице»: все стразы этого размера, этого
    цвета или этого размера и цвета.
  - Новые стразы получают невидимые метки CorelDRAW (размер и цвет) — так плагин узнаёт их, даже если
    имя поменяли вручную. Старые стразы узнаются по имени «ss6 Красный».
  - Preview: сценарий `edit` — картинки «до и после» для каждого режима (`out/preview/edit/`).
  - Установщик — версия 0.2.0.
  - 20 новых тестов (197 всего).

- Этап 4 (в работе):
  - **Новые методы по линии** (раздел 4 ТЗ), вкладка «Линия»:
    - **Каллиграфия** (L4) — число рядов меняется вдоль линии: один ряд на концах и все в середине,
      или от начала к концу («Где шире всего»: в середине / к концу / в начале).
    - **Переход размера** (L5) — «С размера» → «До размера» через все размеры таблицы между ними,
      например ss16 → ss10 → ss6 → ss5. У конца линии нет дырки.
    - **Чередование размеров** (L6) — по шаблону, например «ss6, ss6, ss10». Размеры, которых нет в
      таблице, подсвечиваются, и «Создать» не сработает.
    - **Пунктир** (L7) — «Камней в группе» и «Пропуск».
    - **Акценты** (L8) — крупный камень («Размер акцента») на концах линии, в острых углах или там и там.
  - **«Вокруг линии»: размер крайних рядов** — например, центр ss10, края ss6 (раздел 4, L2).
  - Каждый камень в документе получает имя своего настоящего размера («ss16 Красный», «ss6 Красный»…).
  - Исправлено в ядре: угловой камень больше не пропадает, если соседей рядом с ним пришлось убрать.
  - 11 новых тестов (208 всего).
  - **Новые заливки** (раздел 5 ТЗ), вкладка «Заливка»:
    - **По центральной линии** (F6) — ряды вдоль середины формы, симметрично (стебли, лепестки, буквы).
    - **Переход между кривыми** (F7) — выделите две линии: ряды плавно перетекают от одной к другой.
    - **По направляющей** (F8) — выделите замкнутую форму и линию: ряды внутри формы повторяют её изгиб.
    - **От центра** (F9) — кольца или спираль от самой середины формы.
    - **Случайная плотная** (F10) — без наложений; «Смешать размеры» (например «ss6, ss10») и
      «Вариант» (другой номер — другая раскладка, тот же — та же самая).
    - **Градиент размера** (F11) — крупные в центре, мелкие к краю (или слева направо).
    - **Добивка щелей** (F12) — соты основным камнем, в щели у края — мелкие камни.
  - **Автоподбор сетки** (раздел 5, [NEW]) у «Сетки» и «Сот»: «Сдвиг» или «Сдвиг и поворот» — плагин
    сам ищет раскладку, где у края больше целых камней (на звезде в Preview: 249 вместо 235).
  - Во всех новых заливках отверстия формы остаются пустыми и нет наложений (проверено тестами и
    картинками Preview на звезде, сердце, квадрате).
  - 14 новых тестов (222 всего).
  - **Живой предпросмотр** (раздел 5, [NEW]) — галочка под «Предпросмотр»: картинка пересчитывается
    сама при каждом изменении параметров, камня или метода (фигура — та, что была выделена при нажатии
    «Предпросмотр»). Документ не меняется.
  - **«Живые» стразы** (раздел 8, [NEW]) — каждая группа Strassio помнит, из какой линии/формы, каким
    методом, камнем и параметрами она сделана. Поправили линию → выделите группу → «Правка» →
    «Обновить живые стразы» — камни перестроятся. Рецепт сохраняется вместе с файлом .cdr.
  - **Вернуть размер камням** (раздел 8, «масштабирование с сохранением размера») — растянули дизайн
    средствами CorelDRAW, круги стали не того диаметра → «Правка» → кнопка возвращает каждому камню
    диаметр его размера из таблицы, центр остаётся на месте.
  - **Заполнить дырки** (раздел 7.2, [NEW]) — находит пустые места внутри выложенных страз, куда
    помещается выбранный камень, и ставит туда камни (остаются выделенными — сразу видно, куда).
    Пустота снаружи рисунка дыркой не считается.
  - **Пипетка** (раздел 3.4) — «Цвет» → взять размер и цвет с выделенной стразы.
  - **Смешать цвета** (раздел 3.4) — 2–3 цвета: «Случайно, по долям» (70% / 30% — ровно столько) или
    «По очереди, по шаблону» (2 и 1 → К-К-Б-К-К-Б).
  - **Цвета камней — в палитру документа** (раздел 3.4) — все цвета таблицы с названиями, без повторов.
  - Установщик — версия 0.3.0.
  - 14 новых тестов (236 всего).

- Этап 5 (в работе):
  - **Обводка дизайна** (раздел 8) — новый метод на вкладке «Линия»: выделите весь рисунок (можно
    много фигур, группы тоже) — ряд страз пройдёт снаружи вокруг общего силуэта на «Отступе от края».
    Касающиеся и перекрывающиеся фигуры обводятся как одна; отверстия внутри рисунка и пустоты между
    фигурами не обводятся. Стразы, уже лежащие на рисунке, не мешают.
  - **Производство** (раздел 11), вкладка «Правка»:
    - **Проверить перемычки** — выделяет стразы, между которыми материала меньше заданного
      (по умолчанию 0,3 мм), и пишет, сколько таких мест и какое самое тонкое.
    - **Разложить по слоям по размерам** — слои «Strassio ss6», «Strassio ss10»… — удобно для ArtCAM.
    - **Чистые круги для резки** — без заливки, волосяная обводка, группы Strassio разгруппированы
      (свои группы пользователя не трогаются).
  - 7 новых тестов (243 всего).
  - **Пресеты и «Недавние»** (раздел 8) — строка «Пресет» над методом: сохранить текущие настройки
    (метод, камень, все параметры) под именем, например «Стебель тонкий», и потом выбрать одним
    кликом. В том же списке — 5 последних настроек, с которыми создавали стразы. Файлы — папка
    `presets` рядом с настройками.
  - **Несколько таблиц камней** (раздел 3.1, «наборы таблиц») — в окне таблицы камней вверху «Набор»:
    добавить (копия текущего), переименовать, удалить. Если наборов больше одного, в докере над
    размером появляется «Набор» — переключение одним кликом.
  - Установщик — версия 0.4.0.
  - 5 новых тестов (248 всего).
  - **Вкладка «Векторы»** (раздел 10) — подготовка линий, работает с выделенными фигурами:
    - **Смещение контура** наружу или внутрь на заданное расстояние (углы острые или круглые).
    - **Параллельные линии** — N копий через шаг, в одну или обе стороны.
    - **Разрезать на равные части** — кривые Безье режутся точно, форма не меняется.
    - **Соединить близкие концы**, **Замкнуть**, **Найти разрывы** (красные кружки там, где концы
      почти сходятся).
    - **Развернуть** направление и **Показать начало** линии (откуда пойдёт ряд страз).
    - **Сгладить и упростить** — дрожащая от руки линия становится ровной (лишние узлы убираются,
      через оставшиеся — плавная кривая).
    - **Центральная линия из формы или буквы** — одна линия посередине штриха.
    - **Проверить линии** — самопересечения и микросегменты помечаются, крошечный мусор и дубли
      выделяются.
  - Поля длин на вкладках «Векторы» и «Правка» хранятся в мм и показываются в текущих единицах.
  - Новый тест: в файлах языков нет повторяющихся ключей.
  - 19 новых тестов (267 всего).
  - Установщик — версия 0.5.0. **Этап 5 закрыт.**

- Исправления после скриншотов автора (зазор 0):
  - **«По линии»** на прямоугольнике больше не рвётся: шаг не сжимается так, чтобы стразы налезали,
    лишние стразы не удаляются, у углов нет пустот. В острых углах несколько страз с каждой стороны
    плавно раздвигаются вдоль линии (так же и в «Пунктире»).
  - **«Кант»** при зазоре 0 больше не теряет почти все стразы (касающиеся стразы — это норма, а не
    наложение).
  - **Заливки** (соты, по контуру, от центра) при зазоре 0 — плотные, стразы не выпадают через одну.
  - **«От центра» — спираль и кольца**: у центра стразы больше не налезают и не выпадают дырками.
  - Новый большой тест: все методы × прямоугольник, звезда, сердце, круг × зазор 0 и 0,2 мм —
    стразы не налезают, заливки плотные, ряды без дыр (32 новых теста, 299 всего).
  - Этап 6 начат: основа лицензий (код компьютера, проверка подписи, сроки, 30 дней без интернета) —
    пока только в коде и тестах, в плагине ничего не меняется. 7 новых тестов (306 всего).
  - Установщик — версия 0.5.1.

- Этап 6 — лицензия в плагине (версия 0.6.0):
  - В шапке докера — кнопка-ключ, она открывает окно **«Лицензия»**. В окне: состояние, код этого
    компьютера (кнопка «Копировать»), поле для ключа STRS-XXXX-XXXX-XXXX, «Пробный период 14 дней»,
    «Проверить сейчас», «Освободить этот компьютер», «Файл лицензии…» (для компьютера без интернета).
  - Ключ занят другим компьютером — плагин сам предлагает перенести лицензию (3 раза в год).
  - Раз в 14 дней плагин тихо сверяется с сервером. Без интернета работает 30 дней.
  - **Защита от взлома** (попробовал взломать сам, каждую попытку превратил в тест — все закрыты):
    перевод часов назад (и после окончания пробного периода, и чтобы растянуть 30 дней без
    интернета), часы раньше выдачи лицензии, удаление или правка «меток времени», запрет их записи,
    копия license.json после «Освободить» или переноса на другой ПК, подставной сервер со старым
    ответом. Если часы перевели назад, создавать стразы нельзя, пока не поставят верное время и
    плагин не сверится с сервером. Честный пользователь, который случайно поставил не тот год,
    после исправления часов и «Проверить сейчас» снова работает.
  - Файл офлайн-активации из админки теперь помечен «офлайн»: сверка раз в 14 дней ему не нужна,
    действует до своего срока.
  - **Пока это бета:** проверка лицензии выключена, стразы создаются и без ключа. Включится, когда
    сервер будет запущен в интернете (см. «Что нужно от автора» ниже).
  - Для проверки без Vercel: `npm run local` в папке server — сервер на своём компьютере.
  - 28 новых тестов (333 всего), плюс сквозная проверка «плагин ↔ настоящий сервер».
  - Что нужно от автора, чтобы включить лицензии: аккаунт Vercel и база Neon (бесплатно);
    закрытый ключ подписи лежит в `Документы\Strassio-secrets\LICENSE_PRIVATE_KEY.txt` —
    его вставить в Vercel как переменную `LICENSE_PRIVATE_KEY`, никому не отправлять, сделать копию.

- Версия 0.6.1:
  - Лицензия сверяется с сервером **при каждом запуске CorelDRAW** (раньше — раз в 14 дней).
    Сверка идёт в фоне, докер открывается сразу. Если лицензию перенесли на другой компьютер или
    заблокировали — она снимается при первом же запуске. Нет интернета — работает до 30 дней с
    последней удачной сверки. Файл офлайн-активации не сверяется. 3 новых теста (336 всего).

- Версия 0.6.2:
  - Если CorelDRAW не закрывают, лицензия сверяется с сервером **каждый день** (было — раз в 14 дней).
  - Без интернета плагин работает **14 дней** с последней удачной сверки (было 30). Потом создавать
    стразы нельзя, пока не появится интернет.

- Сервер лицензий запущен в интернете: **https://strassio.vercel.app** (Vercel Hobby + база Neon,
  бесплатно). Админка (генератор ключей): https://strassio.vercel.app/admin — вход паролем автора.
  Проверено через интернет: сервер отвечает, неверный пароль админки отклоняется, пробная лицензия
  выдаётся и её подпись сходится с ключом в плагине. Проверка лицензии в плагине пока выключена —
  включается отдельной командой автора.

- Сайт **https://strassio.vercel.app** (главная и страница «Моя лицензия»):
  - Главная: что умеет плагин, примеры, как начать, вопросы и ответы, кнопка «Скачать».
  - **Ключ восстановления** (RCV-XXXX-XXXX-XXXX-XXXX) — второй секретный код к каждому ключу.
    Админка выдаёт его рядом с ключом; клиенту отдают оба. Для старых ключей — кнопка
    «новый ключ восстановления» в админке (старый код при этом перестаёт работать).
  - Страница **/license** (ключ + ключ восстановления): клиент сам видит свои компьютеры,
    может **освободить** сломанный или старый компьютер (считается переносом, 3 в год) и
    **скачать файл лицензии** для компьютера без интернета. Автору писать не нужно.
  - Защита: неверный ключ восстановления больше 10 раз за 15 минут — адрес ждёт.
  - Админка по-прежнему: https://strassio.vercel.app/admin

- Переносы лицензии (решение автора): **сколько угодно, но не чаще раза в 7 дней** (было 3 в год).
  Если нужно раньше — клиент пишет автору в Telegram, автор жмёт в админке «разрешить перенос сейчас».
  «Освободить» на сайте — тоже перенос. На сайте видно, с какого дня можно следующий перенос.
  Тексты в плагине обновлены (попадут в следующую сборку плагина).

- **Один ключ — один компьютер** (решение автора). В админке убраны поле «Компьютеров» и кнопка
  «число ПК»; старые ключи, где было больше одного компьютера, теперь тоже пускают только один.
- Админка: исправлена ошибка, из-за которой не работала кнопка «Войти»; пароль с любыми буквами.

- Версия 0.7.0 — **активация через сайт** (как просил автор):
  - В окне «Лицензия» три шага: 1) код этого компьютера и «Копировать»; 2) «Открыть сайт
    активации» — браузер откроет страницу https://strassio.vercel.app/activate, код компьютера
    вставится сам; 3) на сайте ввести ключ STRS, скопировать **код активации** (начинается с SA1.)
    и вставить его в окно «Лицензия» → «Активировать».
  - Код активации зашифрован подписью сервера и содержит код компьютера — на другом компьютере
    он не сработает, подделать его нельзя.
  - Ключ в самом плагине больше не вводится. Перенос на новый компьютер — тоже на сайте
    (кнопка «Перенести на этот компьютер», раз в 7 дней).
  - Ключ восстановления RCV нужен только на странице «Моя лицензия».

- Версия 0.7.1:
  - **Проверка лицензии включена.** Без лицензии или пробного периода стразы не создаются —
    при нажатии «Создать» откроется окно «Лицензия». Настройки и таблица камней доступны всегда.
  - Кнопка «Лицензия» переехала из шапки докера в **Настройки** (шестерёнка) → «Лицензия…».
    Строка «Нет лицензии» под шапкой докера тоже открывает это окно.
